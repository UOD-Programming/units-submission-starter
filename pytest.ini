; Do not edit these files. Any changes may result in errors when running the units tests.
; This is the pytest configuration file for ensuring consistent test behavior
; across environments. It enables colored output for better readability.
[pytest]
addopts = --color=yes --import-mode=importlib
testpaths =
    unit_*/tests
